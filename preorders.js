(function(){
'use strict';

var API='https://still-resonance-4f87.swarnerauto.workers.dev';
var STORE_ID='0f9dd4bc-42a7-487e-a972-2905d24513e9';
var SUPABASE_URL='https://vroknjrxubsqyexngwus.supabase.co';
var SUPABASE_KEY='sb_publishable_wbpX2nL8l-4NbXtZNG_bjA_nabSYaJ5';
var SESSION_KEY='mp-foc-session-v1';
var CART_KEY='mp-foc-cart-v1';
var state={catalog:null,cart:readJson(CART_KEY,{}),session:readJson(SESSION_KEY,null),filters:{q:'',publisher:'all',artist:'all',kind:'all'},timer:null};

function readJson(key,fallback){try{return JSON.parse(localStorage.getItem(key)||'null')||fallback;}catch(_){return fallback;}}
function saveJson(key,value){try{localStorage.setItem(key,JSON.stringify(value));}catch(_){}}
function esc(value){return String(value==null?'':value).replace(/[&<>"']/g,function(ch){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch];});}
function money(cents){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(cents||0)/100);}
function dateLabel(value,withTime){if(!value)return'TBA';var d=new Date(value);return new Intl.DateTimeFormat('en-US',{timeZone:'America/Los_Angeles',weekday:'short',month:'short',day:'numeric',year:'numeric',hour:withTime?'numeric':undefined,minute:withTime?'2-digit':undefined,timeZoneName:withTime?'short':undefined}).format(d);}
function unique(values){return Array.from(new Set(values.filter(Boolean))).sort(function(a,b){return a.localeCompare(b);});}
function token(){return state.session&&state.session.access_token||'';}
function api(path,options){options=options||{};var headers=Object.assign({'Content-Type':'application/json'},options.headers||{});if(options.auth!==false&&token())headers.Authorization='Bearer '+token();return fetch(API+path,Object.assign({},options,{headers:headers})).then(async function(response){var data=await response.json().catch(function(){return{};});if(response.status===401&&state.session&&state.session.refresh_token&&!options.retried){await refreshSession();return api(path,Object.assign({},options,{retried:true}));}if(!response.ok)throw new Error(data.error||'The request could not be completed.');return data;});}
function auth(path,body){return fetch(SUPABASE_URL+'/auth/v1/'+path,{method:'POST',headers:{apikey:SUPABASE_KEY,'Content-Type':'application/json'},body:JSON.stringify(body)}).then(async function(response){var data=await response.json().catch(function(){return{};});if(!response.ok)throw new Error(data.msg||data.message||data.error_description||'Account request failed.');return data;});}
function setSession(session){state.session=session&&session.access_token?session:null;if(state.session)saveJson(SESSION_KEY,state.session);else{try{localStorage.removeItem(SESSION_KEY);}catch(_){}}}
async function refreshSession(){if(!state.session||!state.session.refresh_token)return;try{setSession(await auth('token?grant_type=refresh_token',{refresh_token:state.session.refresh_token}));}catch(_){setSession(null);}}
function saveCart(){saveJson(CART_KEY,state.cart);renderFloat();}
function allSkus(){return(state.catalog&&state.catalog.families||[]).flatMap(function(family){return family.variants.map(function(sku){return{family:family,sku:sku};});});}
function findSku(id){var match=allSkus().find(function(entry){return entry.sku.id===id;});return match||null;}
function cartLines(){return Object.keys(state.cart).map(function(id){var match=findSku(id);return match?{family:match.family,sku:match.sku,quantity:Math.max(1,Number(state.cart[id]||1))}:null;}).filter(Boolean);}
function cartTotal(){return cartLines().reduce(function(sum,line){return sum+line.sku.priceCents*line.quantity;},0);}

function mount(){
  if(document.getElementById('mp-foc-app'))return;
  document.body.classList.add('mp-page-preorders');
  if(!document.getElementById('navbarID')){
    var nav=document.createElement('nav');nav.className='mp-foc-nav';nav.setAttribute('aria-label','Main navigation');
    nav.innerHTML='<a class="mp-foc-brand" href="/" aria-label="The Mana Pocket home"><img src="https://cdn.prod.website-files.com/65b15ee0228d06647ca7e4ce/6a7ce98ab3d4819b7565620e_the_mana_pocket_patch_1024x1024.png" alt="The Mana Pocket"></a><div><a href="/">Home</a><a href="/shop">Shop</a><button type="button" data-foc-theme>Pick a theme</button></div>';
    document.body.prepend(nav);nav.querySelector('[data-foc-theme]').addEventListener('click',function(){if(window.WO&&typeof window.WO.openTheme==='function')window.WO.openTheme();});
  }
  var app=document.createElement('main');app.id='mp-foc-app';app.innerHTML='<div class="mp-foc-shell"><div class="mp-foc-loading"><b>Opening the pull box…</b><span>Loading this week’s comic covers.</span></div></div>';
  var footer=document.querySelector('.footer-section,.Footer,.footer');
  if(footer)footer.parentNode.insertBefore(app,footer);else document.body.appendChild(app);
  loadCatalog();
}

async function loadCatalog(){
  try{var data=await api('/public/preorders?store_id='+encodeURIComponent(STORE_ID),{auth:false});state.catalog=data;render();}
  catch(error){document.getElementById('mp-foc-app').innerHTML='<div class="mp-foc-shell"><div class="mp-foc-empty"><h1>Comic preorders are getting bagged and boarded.</h1><p>'+esc(error.message)+'</p><a class="mp-foc-button ghost" href="/">Back to the shop</a></div></div>';}
}

function countdown(){
  if(!state.catalog)return'';var end=new Date(state.catalog.cycle.customer_cutoff_at).getTime();var delta=Math.max(0,end-Date.now());var parts=[['DAYS',Math.floor(delta/86400000)],['HRS',Math.floor(delta%86400000/3600000)],['MIN',Math.floor(delta%3600000/60000)],['SEC',Math.floor(delta%60000/1000)]];return parts.map(function(part){return'<b>'+String(part[1]).padStart(2,'0')+'<small>'+part[0]+'</small></b>';}).join('');
}
function filteredFamilies(){
  var q=state.filters.q.toLowerCase();return state.catalog.families.map(function(family){var variants=family.variants.filter(function(sku){var hay=[family.title,family.seriesName,family.writer,family.publisher,sku.variantLabel,sku.coverArtist,sku.description].join(' ').toLowerCase();return(!q||hay.indexOf(q)!==-1)&&(state.filters.publisher==='all'||family.publisher===state.filters.publisher)&&(state.filters.artist==='all'||sku.coverArtist===state.filters.artist)&&(state.filters.kind==='all'||(state.filters.kind==='first'&&family.isFirstIssue)||(state.filters.kind==='incentive'&&sku.isIncentive)||(state.filters.kind==='standard'&&!sku.isIncentive));});return Object.assign({},family,{variants:variants});}).filter(function(family){return family.variants.length;});
}
function render(){
  var cycle=state.catalog.cycle;var publishers=unique(state.catalog.families.map(function(f){return f.publisher;}));var artists=unique(allSkus().map(function(e){return e.sku.coverArtist;}));var families=filteredFamilies();
  document.getElementById('mp-foc-app').innerHTML='<div class="mp-foc-shell">'+
    '<header><div class="mp-foc-eyebrow">The Mana Pocket · Penguin Random House FOC</div><h1 class="mp-foc-title">Pick your exact covers.</h1><p class="mp-foc-intro">Prepay for the comics and covers you actually want. Regular covers are locked in now. Ratio incentives stay request-only until we have secured copies.</p><div class="mp-foc-deadline"><div><strong>'+(cycle.isOpen?'Orders close '+esc(dateLabel(cycle.customer_cutoff_at,true)):'This FOC is closed')+'</strong><span>We place the distributor order every Monday · quantities and covers are exact</span></div><div class="mp-foc-countdown" data-foc-countdown>'+countdown()+'</div></div></header>'+
    '<section class="mp-foc-controls" aria-label="Filter comic preorders"><input data-filter="q" type="search" placeholder="Search title, creator, character…" value="'+esc(state.filters.q)+'"><select data-filter="publisher"><option value="all">All publishers</option>'+publishers.map(function(v){return'<option'+(state.filters.publisher===v?' selected':'')+'>'+esc(v)+'</option>';}).join('')+'</select><select data-filter="artist"><option value="all">All cover artists</option>'+artists.map(function(v){return'<option'+(state.filters.artist===v?' selected':'')+'>'+esc(v)+'</option>';}).join('')+'</select><select data-filter="kind"><option value="all">All covers</option><option value="standard"'+(state.filters.kind==='standard'?' selected':'')+'>Standard covers</option><option value="first"'+(state.filters.kind==='first'?' selected':'')+'>#1 issues</option><option value="incentive"'+(state.filters.kind==='incentive'?' selected':'')+'>Incentives</option></select><button class="mp-foc-button ghost" data-account>'+(token()?'My preorders':'Sign in')+'</button></section>'+
    '<div class="mp-foc-result-line"><span>'+families.length+' titles · '+families.reduce(function(n,f){return n+f.variants.length;},0)+' exact covers</span><span>FOC '+esc(dateLabel(cycle.foc_date,false))+'</span></div><div data-foc-families>'+familyHtml(families)+'</div></div><button class="mp-foc-button mp-foc-float" data-cart>My picks <span>'+cartLines().reduce(function(n,l){return n+l.quantity;},0)+'</span></button>';
  bind();if(state.timer)clearInterval(state.timer);state.timer=setInterval(function(){var node=document.querySelector('[data-foc-countdown]');if(node)node.innerHTML=countdown();},1000);
}
function familyHtml(families){if(!families.length)return'<div class="mp-foc-empty"><h2>No covers match that search.</h2><p>Try clearing a filter.</p></div>';return families.map(function(family){var badges=(family.isFirstIssue?'<span class="mp-foc-badge hot">Issue #1</span>':'')+(family.isNewSeries?'<span class="mp-foc-badge">New series</span>':'')+(family.variants.some(function(v){return v.isIncentive;})?'<span class="mp-foc-badge ratio">Incentives</span>':'');return'<article class="mp-foc-family"><header class="mp-foc-family-head"><div><h2>'+esc(family.seriesName||family.title)+(family.issueNumber?' #'+esc(family.issueNumber):'')+'</h2><p>'+esc([family.writer&&'Written by '+family.writer,family.interiorArtist&&'Art by '+family.interiorArtist].filter(Boolean).join(' · '))+'</p><div class="mp-foc-badges">'+badges+'</div></div><div class="mp-foc-family-meta">'+esc(family.publisher)+'<br>On sale '+esc(dateLabel(family.onSaleDate,false))+'<br>'+family.variants.length+' cover'+(family.variants.length===1?'':'s')+'</div></header><div class="mp-foc-variants">'+family.variants.map(function(sku){return coverHtml(family,sku);}).join('')+'</div></article>';}).join('');}
function coverHtml(family,sku){var image=sku.coverImageUrl?'<img loading="lazy" decoding="async" src="'+esc(sku.coverImageUrl)+'" alt="'+esc((family.seriesName||family.title)+' '+sku.variantLabel)+'">':'<span>No cover image yet</span>';var ratio=sku.isIncentive?'<div class="mp-foc-ratio">'+esc(sku.orderRequirement||('1:'+sku.ratioThreshold+' incentive'))+'<div class="mp-foc-progress"><i style="width:'+Math.min(100,100*Number(sku.qualification&&sku.qualification.total||0)/Math.max(1,Number(sku.ratioThreshold||1)))+'%"></i></div>'+(sku.waitlistOnly?'Request list only · no charge':'Secured copies available')+'</div>':'';var action=sku.waitlistOnly?'Request one':'Add exact cover';return'<section class="mp-foc-cover"><button class="mp-foc-cover-image" data-lightbox="'+esc(sku.id)+'" aria-label="Enlarge cover">'+image+'</button><h3>'+esc(sku.variantLabel||'Cover A')+'</h3><div class="artist">'+esc(sku.coverArtist?'Cover by '+sku.coverArtist:'Cover artist not listed')+'</div><div class="price">'+money(sku.priceCents)+'</div><div class="release">UPC '+esc(sku.upc)+'<br>On sale '+esc(dateLabel(sku.onSaleDate,false))+'</div>'+ratio+'<div class="actions"><button class="mp-foc-button" data-'+(sku.waitlistOnly?'waitlist':'add')+'="'+esc(sku.id)+'" '+(!state.catalog.cycle.isOpen?'disabled':'')+'>'+action+'</button><input class="mp-foc-qty" data-qty="'+esc(sku.id)+'" type="number" value="1" min="1" max="50" aria-label="Quantity"></div></section>';}

function bind(){
  document.querySelectorAll('[data-filter]').forEach(function(control){control.addEventListener(control.tagName==='INPUT'?'input':'change',function(){state.filters[control.dataset.filter]=control.value;var families=filteredFamilies();document.querySelector('[data-foc-families]').innerHTML=familyHtml(families);var line=document.querySelector('.mp-foc-result-line span');if(line)line.textContent=families.length+' titles · '+families.reduce(function(n,f){return n+f.variants.length;},0)+' exact covers';bindDynamic();});});bindDynamic();
  document.querySelector('[data-cart]')?.addEventListener('click',openCart);document.querySelector('[data-account]')?.addEventListener('click',openAccount);
}
function bindDynamic(){
  document.querySelectorAll('[data-add]').forEach(function(button){button.addEventListener('click',function(){var input=document.querySelector('[data-qty="'+button.dataset.add+'"]');state.cart[button.dataset.add]=Math.min(50,(Number(state.cart[button.dataset.add]||0)+Math.max(1,Number(input&&input.value||1))));saveCart();button.textContent='Added ✓';setTimeout(function(){button.textContent='Add exact cover';},900);});});
  document.querySelectorAll('[data-waitlist]').forEach(function(button){button.addEventListener('click',function(){requestWaitlist(button.dataset.waitlist,Number(document.querySelector('[data-qty="'+button.dataset.waitlist+'"]')?.value||1));});});
  document.querySelectorAll('[data-lightbox]').forEach(function(button){button.addEventListener('click',function(){var match=findSku(button.dataset.lightbox);if(match)dialog('<div class="mp-foc-lightbox"><img src="'+esc(match.sku.coverImageUrl)+'" alt=""><p>'+esc((match.family.seriesName||match.family.title)+' · '+match.sku.variantLabel)+'</p></div>','wide');});});
}
function renderFloat(){var button=document.querySelector('[data-cart]');if(button)button.innerHTML='My picks <span>'+cartLines().reduce(function(n,l){return n+l.quantity;},0)+'</span>';}
function dialog(content,className){closeDialog();var overlay=document.createElement('div');overlay.className='mp-foc-overlay';overlay.innerHTML='<section class="mp-foc-dialog '+(className||'')+'" role="dialog" aria-modal="true"><button class="mp-foc-close" aria-label="Close">×</button>'+content+'</section>';document.body.appendChild(overlay);overlay.querySelector('.mp-foc-close').addEventListener('click',closeDialog);overlay.addEventListener('click',function(event){if(event.target===overlay)closeDialog();});document.addEventListener('keydown',escapeDialog);return overlay;}
function closeDialog(){document.querySelector('.mp-foc-overlay')?.remove();document.removeEventListener('keydown',escapeDialog);}
function escapeDialog(event){if(event.key==='Escape')closeDialog();}
function status(node,message,kind){node.innerHTML='<div class="mp-foc-status '+(kind||'')+'">'+esc(message)+'</div>';}

function openCart(){var lines=cartLines();var html='<h2>My exact-cover picks</h2><div data-cart-lines>'+(lines.length?lines.map(function(line){return'<div class="mp-foc-cart-line"><img src="'+esc(line.sku.coverImageUrl)+'" alt=""><div><h3>'+esc((line.family.seriesName||line.family.title)+' · '+line.sku.variantLabel)+'</h3><p>Qty '+line.quantity+' · '+money(line.sku.priceCents)+' each<br>UPC '+esc(line.sku.upc)+'</p></div><button data-remove="'+esc(line.sku.id)+'" aria-label="Remove">×</button></div>';}).join(''):'<p>Your pull box is empty.</p>')+'</div><div class="mp-foc-cart-total"><span>Books subtotal</span><span>'+money(cartTotal())+'</span></div><div class="mp-foc-cart-actions"><button class="mp-foc-button ghost" data-close-cart>Keep looking</button><button class="mp-foc-button" data-checkout '+(!lines.length?'disabled':'')+'>Secure these covers</button></div>';var overlay=dialog(html);overlay.querySelectorAll('[data-remove]').forEach(function(button){button.addEventListener('click',function(){delete state.cart[button.dataset.remove];saveCart();openCart();});});overlay.querySelector('[data-close-cart]').addEventListener('click',closeDialog);overlay.querySelector('[data-checkout]').addEventListener('click',beginCheckout);}
function requireSession(next){if(token()){next();return;}openAuth(next);}
function openAuth(next){var overlay=dialog('<h2>Collector sign in</h2><p>Use one account to pay, check your preorder status, and cancel before the cutoff.</p><form class="mp-foc-auth" data-auth-form><input name="name" placeholder="Name (new collectors)"><input name="email" type="email" required placeholder="Email"><input name="password" type="password" minlength="8" required placeholder="Password · 8+ characters"><div data-auth-status></div><div class="mp-foc-cart-actions"><button class="mp-foc-button ghost" type="button" data-sign-up>Create account</button><button class="mp-foc-button" type="submit">Sign in</button></div></form>');var form=overlay.querySelector('[data-auth-form]'),out=overlay.querySelector('[data-auth-status]');async function perform(kind){var data=new FormData(form),email=String(data.get('email')||'').trim(),password=String(data.get('password')||''),name=String(data.get('name')||'').trim();if(!email||password.length<8){status(out,'Enter an email and a password with at least 8 characters.','error');return;}status(out,'Opening your pull box…');try{var session;if(kind==='signup'){var result=await auth('signup',{email:email,password:password,data:{full_name:name}});if(!result.access_token){status(out,'Check your email to confirm the account, then sign in.','success');return;}session=result;}else session=await auth('token?grant_type=password',{email:email,password:password});setSession(session);closeDialog();render();if(next)next();}catch(error){status(out,error.message,'error');}}form.addEventListener('submit',function(event){event.preventDefault();perform('signin');});overlay.querySelector('[data-sign-up]').addEventListener('click',function(){perform('signup');});}

function requestWaitlist(skuId,quantity){requireSession(async function(){var match=findSku(skuId);var overlay=dialog('<h2>Request this incentive</h2><p>'+esc((match.family.seriesName||match.family.title)+' · '+match.sku.variantLabel)+'</p><p>This is a request, not a purchase. You will not be charged unless The Mana Pocket secures a copy and offers it to you.</p><div data-request-status></div><button class="mp-foc-button" data-confirm-request>Join request list</button>');var out=overlay.querySelector('[data-request-status]');overlay.querySelector('[data-confirm-request]').addEventListener('click',async function(){try{status(out,'Saving your request…');var result=await api('/public/preorders/waitlist',{method:'POST',body:JSON.stringify({storeId:STORE_ID,skuId:skuId,quantity:quantity})});status(out,result.message,'success');}catch(error){status(out,error.message,'error');}});});}

function beginCheckout(){requireSession(openCheckout);}
function openCheckout(){var html='<h2>Pickup or shipping</h2><form class="mp-foc-checkout" data-checkout-form><div class="mp-foc-form-grid"><input name="name" required placeholder="Collector name"><input name="phone" placeholder="Phone"><select name="method"><option value="pickup">Pick up at The Mana Pocket</option><option value="shipping">Ship to me · live carrier rate</option></select><span></span></div><div class="mp-foc-form-grid" data-address hidden><input name="line1" placeholder="Street address"><input name="line2" placeholder="Apt / suite"><input name="city" placeholder="City"><input name="state" maxlength="2" placeholder="State"><input name="zip" placeholder="ZIP code"><button class="mp-foc-button ghost" type="button" data-quote>Get live rates</button></div><div data-rates></div><div class="mp-foc-cart-total"><span>Books subtotal</span><span>'+money(cartTotal())+'</span></div><div data-checkout-status></div><button class="mp-foc-button" type="submit">Continue to secure payment</button></form>';var overlay=dialog(html);var form=overlay.querySelector('form'),methodSelect=form.elements.namedItem('method'),address=overlay.querySelector('[data-address]'),rates=overlay.querySelector('[data-rates]'),out=overlay.querySelector('[data-checkout-status]');methodSelect.addEventListener('change',function(){address.hidden=methodSelect.value!=='shipping';rates.innerHTML='';});overlay.querySelector('[data-quote]').addEventListener('click',async function(){var data=new FormData(form);if(!data.get('line1')||!data.get('city')||!data.get('state')||!data.get('zip')){status(rates,'Complete the shipping address first.','error');return;}status(rates,'Checking current carrier rates…');try{var result=await api('/public/shipping/quotes',{method:'POST',auth:false,body:JSON.stringify({storeId:STORE_ID,quantity:cartLines().reduce(function(n,l){return n+l.quantity;},0),address:{name:data.get('name'),phone:data.get('phone'),line1:data.get('line1'),line2:data.get('line2'),city:data.get('city'),state:data.get('state'),zip:data.get('zip')}})});rates.innerHTML=result.rates.map(function(rate,index){return'<label class="mp-foc-status"><input type="radio" name="shippingRate" value="'+esc(rate.rateId)+'" data-rate-cents="'+rate.amountCents+'" '+(index===0?'checked':'')+'> <strong>'+esc(rate.carrier+' '+rate.service)+'</strong> · '+money(rate.amountCents)+(rate.estimatedDays?' · about '+rate.estimatedDays+' days':'')+'</label>';}).join('');}catch(error){status(rates,error.message,'error');}});form.addEventListener('submit',async function(event){event.preventDefault();var data=new FormData(form),method=data.get('method'),chosen=form.querySelector('[name="shippingRate"]:checked');if(method==='shipping'&&!chosen){status(out,'Get and choose a live shipping rate first.','error');return;}status(out,'Creating your secure preorder…');var fulfillment={method:method,name:data.get('name'),phone:data.get('phone')};if(method==='shipping'){fulfillment.shippingRateId=chosen.value;fulfillment.shippingAddress={name:data.get('name'),phone:data.get('phone'),line1:data.get('line1'),line2:data.get('line2'),city:data.get('city'),state:data.get('state'),zip:data.get('zip')};}try{var result=await api('/public/preorders/checkout',{method:'POST',body:JSON.stringify({storeId:STORE_ID,cycleId:state.catalog.cycle.id,items:cartLines().map(function(line){return{skuId:line.sku.id,quantity:line.quantity};}),fulfillment:fulfillment})});openPayment(result);}catch(error){status(out,error.message,'error');}});}

async function loadStripe(){if(window.Stripe)return window.Stripe;if(!document.querySelector('script[data-mp-stripe]')){var script=document.createElement('script');script.src='https://js.stripe.com/v3/';script.dataset.mpStripe='';document.head.appendChild(script);}for(var i=0;i<100&&!window.Stripe;i++)await new Promise(function(resolve){setTimeout(resolve,50);});if(!window.Stripe)throw new Error('Secure payment could not load.');return window.Stripe;}
async function openPayment(order){var overlay=dialog('<h2>Secure payment</h2><p>Order '+esc(order.orderNumber)+' · '+money(order.amountCents)+'</p><div id="mp-foc-payment"></div><div data-payment-status></div><button class="mp-foc-button" data-pay>Pay and place preorder</button>');var out=overlay.querySelector('[data-payment-status]');try{var Stripe=await loadStripe(),stripe=Stripe(order.publishableKey),elements=stripe.elements({clientSecret:order.clientSecret}),payment=elements.create('payment');payment.mount('#mp-foc-payment');overlay.querySelector('[data-pay]').addEventListener('click',async function(){status(out,'Confirming payment…');var result=await stripe.confirmPayment({elements:elements,confirmParams:{return_url:location.origin+'/preorders?payment=success'},redirect:'if_required'});if(result.error){status(out,result.error.message,'error');return;}state.cart={};saveCart();status(out,'Payment received. Your exact covers are in the pull box.','success');setTimeout(openAccount,900);});}catch(error){status(out,error.message,'error');}}

function openAccount(){requireSession(async function(){var overlay=dialog('<h2>My preorders</h2><div data-account-status><div class="mp-foc-loading"><b>Checking the pull box…</b></div></div><div class="mp-foc-cart-actions"><button class="mp-foc-button ghost" data-sign-out>Sign out</button></div>','wide');overlay.querySelector('[data-sign-out]').addEventListener('click',function(){setSession(null);closeDialog();render();});var out=overlay.querySelector('[data-account-status]');try{var result=await api('/public/preorders/my?store_id='+encodeURIComponent(STORE_ID));out.innerHTML=result.orders.length?result.orders.map(orderHtml).join(''):'<p>You do not have any comic preorders yet.</p>';out.querySelectorAll('[data-cancel-order]').forEach(function(button){button.addEventListener('click',async function(){if(!confirm('Cancel and refund this preorder before FOC closes?'))return;button.disabled=true;try{await api('/public/preorders/cancel',{method:'POST',body:JSON.stringify({storeId:STORE_ID,orderId:button.dataset.cancelOrder})});openAccount();}catch(error){alert(error.message);button.disabled=false;}});});}catch(error){status(out,error.message,'error');}});}
function orderHtml(order){return'<article class="mp-foc-account-order"><header><h3>'+esc(order.order_number)+'</h3><span class="status">'+esc(order.status.replaceAll('_',' '))+'</span></header><p>'+money(order.total_cents)+' · '+esc(order.fulfillment_method)+' · ordered '+esc(dateLabel(order.created_at,true))+'</p><div class="mp-foc-account-items">'+(order.items||[]).map(function(item){return'<img src="'+esc(item.sku_snapshot&&item.sku_snapshot.coverImageUrl||'')+'" title="'+esc(item.sku_snapshot&&item.sku_snapshot.title||'')+'" alt="">';}).join('')+'</div>'+(order.canCancel?'<button class="mp-foc-button ghost" data-cancel-order="'+esc(order.id)+'">Cancel before cutoff</button>':'')+'</article>';}

if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();
})();
